A simple python script to test DSPy with GEPA optimizer.

recommend python=3.12

Running commands:

python -m pip install -r requirements.txt

python -m spacy download en_core_web_md

python testMetrics.py
